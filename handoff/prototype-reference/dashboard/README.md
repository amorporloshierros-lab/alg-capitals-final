# Student Dashboard UI Kit — KeyRules × ALG Capitals

Recreates the logged-in student portal at `/dashboard`.

## Components
- `Header.jsx` — Welcome bar with user button and "Cuenta Activa" chip.
- `DashCard.jsx` — Soft emerald-bordered panel (`rounded-xl`, emerald/20 hover → /40).
- `BiasCard.jsx` — Daily market bias video slot + quote strip.
- `TrainingCard.jsx` — Module progress + progress bar.
- `FunderSync.jsx` — FTMO / Alpha Capital prop-firm status rows.
- `ResourcesCard.jsx` — Elite resource links.
- `VipCard.jsx` — Discord community CTA.
